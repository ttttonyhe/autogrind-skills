---
description: Start AutoGrind — continuous auto-work mode
---

Use the `Skill` tool to load and follow the `autogrind:autogrind` skill. If the user provided context with this command (e.g., a project path, topic, or focus area), treat it as the initial grind target.
