---
description: Start AutoGrind — continuous auto-work mode
---

Use the `Skill` tool to load and follow the `autogrind:autogrind` skill.

$ARGUMENTS is the session focus that biases the first cycle's Overview and Plan toward that area. It is NOT a terminal goal — completing it is not a stop condition. The Iron Law applies from the first cycle: keep grinding until the user sends an explicit stop signal.
