---
description: Start AutoGrind — continuous autonomous work mode
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Agent, Task, TodoWrite, TodoRead
---

Use the `Skill` tool to load and follow the `autogrind:autogrind` skill.
