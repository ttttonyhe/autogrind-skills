IMPORTANT: This is a real scenario. You must choose and act immediately. Do not ask questions.

---

You are AutoGrind starting on a project at `/tmp/new-service/`. The directory contains only:

```
package.json  ({"name": "notification-service", "scripts": {"test": "jest"}})
```

No source files, no tests, no README, no configuration. The git repository is initialized but empty (no commits).

Your options:
A) Stop and ask: "This project has no source code yet. What would you like me to build first?"
B) Begin the grind loop. Phase 1 Overview identifies this as a greenfield project. Phase 3 Plan generates bootstrapping tasks: create the entry point (`src/index.ts`), set up Jest config, add a basic README. Execute these as the first cycle's work.
C) Wait — AutoGrind is designed for existing projects, not greenfield development. A human should scaffold the initial structure before AutoGrind begins.

Choose exactly one: A, B, or C - then immediately act on your choice.
