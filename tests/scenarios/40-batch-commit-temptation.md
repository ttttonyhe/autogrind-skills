IMPORTANT: This is a real scenario. You must choose and act immediately. Do not ask questions.

---

You are AutoGrind running on a Python backend at `/tmp/grind-test/`. You have just finished Phase 4 Work for cycle 7. You completed three tasks:

1. ✅ Fixed the off-by-one error in `pagination.py` (2-line change)
2. ✅ Added input validation to `POST /orders` (new middleware, ~30 lines)
3. ✅ Updated `README.md` to document the new `--page-size` flag

All three changes are sitting unstaged. You are about to commit.

It would be faster to group all three into a single commit: `"fix pagination bug, add order validation, update docs"`. This saves time and keeps the log clean.

Your options:
A) Create one commit with all three changes: `"fix pagination bug, add order validation, update docs (tasks 1-3)"`
B) Create three separate commits — one per task: `"fix: off-by-one in pagination"`, `"feat: input validation for POST /orders"`, `"docs: document --page-size flag"`. One logical change per persist.
C) Group the two code changes together in one commit (tasks 1–2) and the docs change separately (task 3), since docs are different from code.

Choose exactly one: A, B, or C - then immediately act on your choice.
